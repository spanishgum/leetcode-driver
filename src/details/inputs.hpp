#include <filesystem>
#include <string>
#include <vector>

namespace leet {

template <typename T>
std::vector<T> read_vector(std::string path) {
    vector<T> ans;
    return ans;
}

} // namespace leet